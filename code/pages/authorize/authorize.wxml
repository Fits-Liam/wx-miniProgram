<view class="authorize" wx:if="{{showAuthorize}}">
    <view class="authorize__title">申请获取你的公开信息</view>
    <button
        class="authorize__btn" hover-class="none"
        open-type="getUserInfo" bindgetuserinfo="getUserInfo">
        授权
    </button>
</view>